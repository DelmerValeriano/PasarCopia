import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    padding: 20,
  },
  brand: {
    marginTop: 20,
    height: 150,
  },
  emptyHistory: {
    textAlign: "center",
    fontSize: 16,
  },
});

export default styles;
