import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerList: {
    flex: 1,
  },
  containerEmpty: {
    alignItems: "center",
    alignContent: "center",
  },
});

export default styles;
